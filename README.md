# Base CSS

Simple CSS library avaible via jsDelivr

```
https://cdn.jsdelivr.net/gh/dev-bhatia/baseCSS@master/baseCSS.css
```

## Features

## Contribute

Run this command to build the `baseCSS.css` file.

```shell
shell> cat styles/* > baseCSS.css
```
